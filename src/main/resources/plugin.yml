name: NPC
version: '${project.version}'
main: win.oreo.npc.Main
author: superoreo

commands:
  item:
    aliases:
      - 아이템
  npc:
    aliases:
      - shop
      - 상점
  account:
    aliases:
      - acc
      - 계좌
      - money
      - balance